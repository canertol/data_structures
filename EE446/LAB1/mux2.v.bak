module mux2 #(parameter W = 1)( select, mux_in, mux_out );

input 	  select;
input[W:0] mux_in;
output     mux_out;

wire      mux_out;
wire 		 select;
wire[W:0] mux_in;

assign mux_out = mux_in[select];

endmodule