module shift_reg #(parameter W=8)(D,reset,clk,enable,out);
    input [(W-1):0] D;                 
    input reset,clk,enable;
    output [(W-1):0] out;
    reg [(W-1):0] out;
	 
	 always @(posedge clk) begin
		if (reset)
			out=0;
		else if (enable)
			out=D;
		else
			out=out;
		end
endmodule